const gameServiceEnum = require("../../../enum/game-service-enum");

class GameRepository { 
    //TODO
    /*
    getCurrentPlayer
    setLock
    getPlayerData
    getOponentId
    createGame
    updateGameData
    getGameData
    deleteLock
    updateGameTimeout(90)
    setShips(V2)
    getPlayersWhoSetShips(not need if v2)
    
    */
}
GameRepository.prototype._ENUM = gameServiceEnum;
